<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--  Importante dar permiso para conexión a internet -->
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppInventario"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        >
        <activity
            android:name=".visualizar_estado"
            android:exported="false" />
        <activity
            android:name=".visualizar_mov"
            android:exported="false" />
        <activity
            android:name=".Registrar_Locales"
            android:exported="false" />
        <activity
            android:name=".Locales"
            android:exported="false" />
        <activity
            android:name=".Ingreso_Retiro"
            android:exported="false" />
        <activity
            android:name=".Registrar_Inventario"
            android:exported="false" />
        <activity
            android:name=".menu_seleccionar_accion"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>